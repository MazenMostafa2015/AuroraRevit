"""Educational C# action templates shipped with AuroraRevit v1.8.3.

The Revit add-in reads the same templates from embedded examples.json files.
This registry keeps a Python-accessible copy for tooling and documentation.
"""

ACTION_TEMPLATES = {
    'Create a Door Schedule with Fire Ratings': {'discipline': 'Architecture', 'prompt': 'Create a door schedule named A-DOOR with mark, width, and fire rating columns.', 'code': 'using System;\nusing Autodesk.Revit.DB;\nusing System.Linq;\n\n// AuroraRevit v1.8.3 educational template: Create a Door Schedule with Fire Ratings\n// This template is intentionally read-only or creates a single named view/schedule.\npublic static void Run(Document doc)\n{\n    var category = new ElementId(BuiltInCategory.OST_Doors);\n    var schedule = ViewSchedule.CreateSchedule(doc, category);\n    schedule.Name = "A-DOOR";\n}'},
    'Create an Exterior Wall Schedule': {'discipline': 'Architecture', 'prompt': 'Create a schedule named A-WALL for exterior walls with type, length, and area fields.', 'code': 'using System;\nusing Autodesk.Revit.DB;\nusing System.Linq;\n\n// AuroraRevit v1.8.3 educational template: Create an Exterior Wall Schedule\n// This template is intentionally read-only or creates a single named view/schedule.\npublic static void Run(Document doc)\n{\n    var category = new ElementId(BuiltInCategory.OST_Walls);\n    var schedule = ViewSchedule.CreateSchedule(doc, category);\n    schedule.Name = "A-WALL";\n}'},
    'Find Rooms Missing Department': {'discipline': 'Architecture', 'prompt': 'Report rooms that have no Department value in the active model.', 'code': 'using System;\nusing Autodesk.Revit.DB;\nusing System.Linq;\n\n// AuroraRevit v1.8.3 educational template: Find Rooms Missing Department\n// This template is intentionally read-only or creates a single named view/schedule.\npublic static void Run(Document doc)\n{\n    var category = new ElementId(BuiltInCategory.OST_Rooms);\n    var rooms = new FilteredElementCollector(doc).OfCategory(BuiltInCategory.OST_Rooms).WhereElementIsNotElementType().ToElements();\n    var missing = rooms.Count(room => string.IsNullOrWhiteSpace(room.LookupParameter("Department")?.AsString()));\n}'},
    'Create a Reflected Ceiling Plan': {'discipline': 'Architecture', 'prompt': 'Create a reflected ceiling plan named RCP - Level 1 for Level 1.', 'code': 'using System;\nusing Autodesk.Revit.DB;\nusing System.Linq;\n\n// AuroraRevit v1.8.3 educational template: Create a Reflected Ceiling Plan\n// This template is intentionally read-only or creates a single named view/schedule.\npublic static void Run(Document doc)\n{\n    var category = new ElementId(BuiltInCategory.OST_Ceilings);\n    var level = new FilteredElementCollector(doc).OfClass(typeof(Level)).Cast<Level>().FirstOrDefault(x => x.Name == "Level 1");\n    if (level == null) return;\n    var view = ViewPlan.Create(doc, new ElementId(BuiltInViewFamily.ViewFamilyCeilingPlan), level.Id);\n    view.Name = "RCP - Level 1";\n}'},
    'Count Windows by Type': {'discipline': 'Architecture', 'prompt': 'Count all window instances grouped by family and type.', 'code': 'using System;\nusing Autodesk.Revit.DB;\nusing System.Linq;\n\n// AuroraRevit v1.8.3 educational template: Count Windows by Type\n// This template is intentionally read-only or creates a single named view/schedule.\npublic static void Run(Document doc)\n{\n    var category = new ElementId(BuiltInCategory.OST_Windows);\n    var windows = new FilteredElementCollector(doc).OfCategory(BuiltInCategory.OST_Windows).WhereElementIsNotElementType().ToElements();\n    var counts = windows.GroupBy(x => x.GetTypeId()).Select(g => new { TypeId = g.Key, Count = g.Count() }).ToList();\n}'},
    'Create an Area Plan View': {'discipline': 'Architecture', 'prompt': 'Create an area plan view for the gross building area scheme.', 'code': 'using System;\nusing Autodesk.Revit.DB;\nusing System.Linq;\n\n// AuroraRevit v1.8.3 educational template: Create an Area Plan View\n// This template is intentionally read-only or creates a single named view/schedule.\npublic static void Run(Document doc)\n{\n    var category = new ElementId(BuiltInCategory.OST_Areas);\n    var areaScheme = new FilteredElementCollector(doc).OfClass(typeof(AreaScheme)).Cast<AreaScheme>().FirstOrDefault();\n    if (areaScheme == null) return;\n}'},
    'Set Room Occupancy Loads': {'discipline': 'Architecture', 'prompt': 'Set the Occupancy parameter to 2 people for selected rooms.', 'code': 'using System;\nusing Autodesk.Revit.DB;\nusing System.Linq;\n\n// AuroraRevit v1.8.3 educational template: Set Room Occupancy Loads\n// This template is intentionally read-only or creates a single named view/schedule.\npublic static void Run(Document doc)\n{\n    var category = new ElementId(BuiltInCategory.OST_Rooms);\n    var rooms = new FilteredElementCollector(doc).OfCategory(BuiltInCategory.OST_Rooms).WhereElementIsNotElementType().ToElements();\n    var editable = rooms.Where(x => x.LookupParameter("Occupancy") != null).ToList();\n}'},
    'Create a Wall Type Schedule': {'discipline': 'Architecture', 'prompt': 'Create a schedule named A-WALL-TYPES for wall types and their widths.', 'code': 'using System;\nusing Autodesk.Revit.DB;\nusing System.Linq;\n\n// AuroraRevit v1.8.3 educational template: Create a Wall Type Schedule\n// This template is intentionally read-only or creates a single named view/schedule.\npublic static void Run(Document doc)\n{\n    var category = new ElementId(BuiltInCategory.OST_Walls);\n    var schedule = ViewSchedule.CreateSchedule(doc, category);\n    schedule.Name = "A-WALL-TYPES";\n}'},
    'List Unplaced Rooms': {'discipline': 'Architecture', 'prompt': 'List all rooms that are not placed in a plan view.', 'code': 'using System;\nusing Autodesk.Revit.DB;\nusing System.Linq;\n\n// AuroraRevit v1.8.3 educational template: List Unplaced Rooms\n// This template is intentionally read-only or creates a single named view/schedule.\npublic static void Run(Document doc)\n{\n    var category = new ElementId(BuiltInCategory.OST_Rooms);\n    var unplaced = new FilteredElementCollector(doc).OfCategory(BuiltInCategory.OST_Rooms).WhereElementIsNotElementType().ToElements().Where(x => x.Location == null).ToList();\n}'},
    'Create a Presentation Sheet': {'discipline': 'Architecture', 'prompt': 'Create a new sheet named A-100 Presentation with the selected title block.', 'code': 'using System;\nusing Autodesk.Revit.DB;\nusing System.Linq;\n\n// AuroraRevit v1.8.3 educational template: Create a Presentation Sheet\n// This template is intentionally read-only or creates a single named view/schedule.\npublic static void Run(Document doc)\n{\n    var category = new ElementId(BuiltInCategory.OST_Sheets);\n    var titleBlock = new FilteredElementCollector(doc).OfCategory(BuiltInCategory.OST_TitleBlocks).WhereElementIsElementType().FirstOrDefault();\n    if (titleBlock == null) return;\n    var sheet = ViewSheet.Create(doc, titleBlock.Id);\n    sheet.SheetNumber = "A-100";\n    sheet.Name = "Presentation";\n}'},
    'Create a Sheet Index': {'discipline': 'General', 'prompt': 'Create a sheet list schedule named SHEET INDEX.', 'code': 'using System;\nusing Autodesk.Revit.DB;\nusing System.Linq;\n\n// AuroraRevit v1.8.3 educational template: Create a Sheet Index\n// This template is intentionally read-only or creates a single named view/schedule.\npublic static void Run(Document doc)\n{\n    var category = new ElementId(BuiltInCategory.OST_Sheets);\n    var schedule = ViewSchedule.CreateSchedule(doc, category);\n    schedule.Name = "SHEET INDEX";\n}'},
    'Read Project Information': {'discipline': 'General', 'prompt': 'Read the project name, number, and status from Project Information.', 'code': 'using System;\nusing Autodesk.Revit.DB;\nusing System.Linq;\n\n// AuroraRevit v1.8.3 educational template: Read Project Information\n// This template is intentionally read-only or creates a single named view/schedule.\npublic static void Run(Document doc)\n{\n    var category = new ElementId(BuiltInCategory.OST_ProjectInformation);\n    var info = new FilteredElementCollector(doc).OfCategory(BuiltInCategory.OST_ProjectInformation).WhereElementIsNotElementType().FirstOrDefault();\n    var projectName = info?.LookupParameter("Project Name")?.AsString();\n}'},
    'Find Unplaced Views on Sheets': {'discipline': 'General', 'prompt': 'Find views that are not placed on any sheet.', 'code': 'using System;\nusing Autodesk.Revit.DB;\nusing System.Linq;\n\n// AuroraRevit v1.8.3 educational template: Find Unplaced Views on Sheets\n// This template is intentionally read-only or creates a single named view/schedule.\npublic static void Run(Document doc)\n{\n    var category = new ElementId(BuiltInCategory.OST_Views);\n    var views = new FilteredElementCollector(doc).OfClass(typeof(View)).Cast<View>().Where(x => !x.IsTemplate && x.ViewType != ViewType.Schedule).ToList();\n}'},
    'Count Model Warnings': {'discipline': 'General', 'prompt': 'Count warnings in the active document and show the total.', 'code': 'using System;\nusing Autodesk.Revit.DB;\nusing System.Linq;\n\n// AuroraRevit v1.8.3 educational template: Count Model Warnings\n// This template is intentionally read-only or creates a single named view/schedule.\npublic static void Run(Document doc)\n{\n    var category = new ElementId(BuiltInCategory.OST_GenericModel);\n    var warnings = doc.GetWarnings();\n    var warningCount = warnings.Count;\n}'},
    'Create a Coordination Workset': {'discipline': 'General', 'prompt': 'Create a user workset named Coordination if it does not exist.', 'code': 'using System;\nusing Autodesk.Revit.DB;\nusing System.Linq;\n\n// AuroraRevit v1.8.3 educational template: Create a Coordination Workset\n// This template is intentionally read-only or creates a single named view/schedule.\npublic static void Run(Document doc)\n{\n    var category = new ElementId(BuiltInCategory.OST_GenericModel);\n    if (!WorksetTable.IsWorksetNameUnique(doc, "Coordination")) return;\n    var workset = Workset.Create(doc, "Coordination");\n}'},
    'Rename the Active View': {'discipline': 'General', 'prompt': 'Rename the active view to Coordination Review.', 'code': 'using System;\nusing Autodesk.Revit.DB;\nusing System.Linq;\n\n// AuroraRevit v1.8.3 educational template: Rename the Active View\n// This template is intentionally read-only or creates a single named view/schedule.\npublic static void Run(Document doc)\n{\n    var category = new ElementId(BuiltInCategory.OST_Views);\n    var view = doc.ActiveView;\n    if (view != null && !view.IsTemplate) view.Name = "Coordination Review";\n}'},
    'Create an Area Schedule': {'discipline': 'General', 'prompt': 'Create a schedule named AREA SUMMARY for areas in the model.', 'code': 'using System;\nusing Autodesk.Revit.DB;\nusing System.Linq;\n\n// AuroraRevit v1.8.3 educational template: Create an Area Schedule\n// This template is intentionally read-only or creates a single named view/schedule.\npublic static void Run(Document doc)\n{\n    var category = new ElementId(BuiltInCategory.OST_Areas);\n    var schedule = ViewSchedule.CreateSchedule(doc, category);\n    schedule.Name = "AREA SUMMARY";\n}'},
    'Find Elements with Empty Mark': {'discipline': 'General', 'prompt': 'Find model elements that have an empty Mark parameter.', 'code': 'using System;\nusing Autodesk.Revit.DB;\nusing System.Linq;\n\n// AuroraRevit v1.8.3 educational template: Find Elements with Empty Mark\n// This template is intentionally read-only or creates a single named view/schedule.\npublic static void Run(Document doc)\n{\n    var category = new ElementId(BuiltInCategory.OST_GenericModel);\n    var elements = new FilteredElementCollector(doc).WhereElementIsNotElementType().ToElements();\n    var unmarked = elements.Where(x => string.IsNullOrWhiteSpace(x.LookupParameter("Mark")?.AsString())).ToList();\n}'},
    'Create a Revision Schedule': {'discipline': 'General', 'prompt': 'Create a revision schedule named REVISION LOG.', 'code': 'using System;\nusing Autodesk.Revit.DB;\nusing System.Linq;\n\n// AuroraRevit v1.8.3 educational template: Create a Revision Schedule\n// This template is intentionally read-only or creates a single named view/schedule.\npublic static void Run(Document doc)\n{\n    var category = new ElementId(BuiltInCategory.OST_Revisions);\n    var schedule = ViewSchedule.CreateSchedule(doc, category);\n    schedule.Name = "REVISION LOG";\n}'},
    'Count Elements in the Active View': {'discipline': 'General', 'prompt': 'Count visible model elements in the active view.', 'code': 'using System;\nusing Autodesk.Revit.DB;\nusing System.Linq;\n\n// AuroraRevit v1.8.3 educational template: Count Elements in the Active View\n// This template is intentionally read-only or creates a single named view/schedule.\npublic static void Run(Document doc)\n{\n    var category = new ElementId(BuiltInCategory.OST_GenericModel);\n    var elements = new FilteredElementCollector(doc, doc.ActiveView.Id).WhereElementIsNotElementType().ToElements();\n    var visibleCount = elements.Count;\n}'},
    'Create a Duct Schedule': {'discipline': 'MEP', 'prompt': 'Create a schedule named M-DUCT for all ducts with size, system, and length fields.', 'code': 'using System;\nusing Autodesk.Revit.DB;\nusing System.Linq;\n\n// AuroraRevit v1.8.3 educational template: Create a Duct Schedule\n// This template is intentionally read-only or creates a single named view/schedule.\npublic static void Run(Document doc)\n{\n    var category = new ElementId(BuiltInCategory.OST_DuctCurves);\n    var schedule = ViewSchedule.CreateSchedule(doc, category);\n    schedule.Name = "M-DUCT";\n}'},
    'Create a Cable Tray Schedule': {'discipline': 'MEP', 'prompt': 'Create a schedule named E-CABLETRAY for cable trays.', 'code': 'using System;\nusing Autodesk.Revit.DB;\nusing System.Linq;\n\n// AuroraRevit v1.8.3 educational template: Create a Cable Tray Schedule\n// This template is intentionally read-only or creates a single named view/schedule.\npublic static void Run(Document doc)\n{\n    var category = new ElementId(BuiltInCategory.OST_CableTray);\n    var schedule = ViewSchedule.CreateSchedule(doc, category);\n    schedule.Name = "E-CABLETRAY";\n}'},
    'Select All Pipes': {'discipline': 'MEP', 'prompt': 'Select every pipe instance in the active project.', 'code': 'using System;\nusing Autodesk.Revit.DB;\nusing System.Linq;\n\n// AuroraRevit v1.8.3 educational template: Select All Pipes\n// This template is intentionally read-only or creates a single named view/schedule.\npublic static void Run(Document doc)\n{\n    var category = new ElementId(BuiltInCategory.OST_PipeCurves);\n    var pipes = new FilteredElementCollector(doc).OfCategory(BuiltInCategory.OST_PipeCurves).WhereElementIsNotElementType().ToElementIds();\n}'},
    'Create a Duct Insulation Schedule': {'discipline': 'MEP', 'prompt': 'Create a schedule named M-DUCT-INSULATION for duct insulation elements.', 'code': 'using System;\nusing Autodesk.Revit.DB;\nusing System.Linq;\n\n// AuroraRevit v1.8.3 educational template: Create a Duct Insulation Schedule\n// This template is intentionally read-only or creates a single named view/schedule.\npublic static void Run(Document doc)\n{\n    var category = new ElementId(BuiltInCategory.OST_DuctInsulations);\n    var schedule = ViewSchedule.CreateSchedule(doc, category);\n    schedule.Name = "M-DUCT-INSULATION";\n}'},
    'Find Unconnected Duct Ends': {'discipline': 'MEP', 'prompt': 'Find ducts with open connectors that are not connected to another element.', 'code': 'using System;\nusing Autodesk.Revit.DB;\nusing System.Linq;\n\n// AuroraRevit v1.8.3 educational template: Find Unconnected Duct Ends\n// This template is intentionally read-only or creates a single named view/schedule.\npublic static void Run(Document doc)\n{\n    var category = new ElementId(BuiltInCategory.OST_DuctCurves);\n    var ducts = new FilteredElementCollector(doc).OfCategory(BuiltInCategory.OST_DuctCurves).WhereElementIsNotElementType().ToElements();\n    var openDucts = ducts.Where(x => (x as MEPCurve)?.ConnectorManager?.Connectors.Cast<Connector>().Any(c => !c.IsConnected) == true).ToList();\n}'},
    'Count Fire Dampers': {'discipline': 'MEP', 'prompt': 'Count all fire damper mechanical equipment in the active model.', 'code': 'using System;\nusing Autodesk.Revit.DB;\nusing System.Linq;\n\n// AuroraRevit v1.8.3 educational template: Count Fire Dampers\n// This template is intentionally read-only or creates a single named view/schedule.\npublic static void Run(Document doc)\n{\n    var category = new ElementId(BuiltInCategory.OST_MechanicalEquipment);\n    var dampers = new FilteredElementCollector(doc).OfCategory(BuiltInCategory.OST_MechanicalEquipment).WhereElementIsNotElementType().Where(x => x.Name.IndexOf("fire damper", StringComparison.OrdinalIgnoreCase) >= 0).ToList();\n}'},
    'Create an Electrical Equipment Schedule': {'discipline': 'MEP', 'prompt': 'Create a schedule named E-EQUIPMENT for electrical equipment.', 'code': 'using System;\nusing Autodesk.Revit.DB;\nusing System.Linq;\n\n// AuroraRevit v1.8.3 educational template: Create an Electrical Equipment Schedule\n// This template is intentionally read-only or creates a single named view/schedule.\npublic static void Run(Document doc)\n{\n    var category = new ElementId(BuiltInCategory.OST_ElectricalEquipment);\n    var schedule = ViewSchedule.CreateSchedule(doc, category);\n    schedule.Name = "E-EQUIPMENT";\n}'},
    'List Air Terminals by Flow': {'discipline': 'MEP', 'prompt': 'List air terminals in the active model and read their flow values.', 'code': 'using System;\nusing Autodesk.Revit.DB;\nusing System.Linq;\n\n// AuroraRevit v1.8.3 educational template: List Air Terminals by Flow\n// This template is intentionally read-only or creates a single named view/schedule.\npublic static void Run(Document doc)\n{\n    var category = new ElementId(BuiltInCategory.OST_MechanicalEquipment);\n    var terminals = new FilteredElementCollector(doc).OfCategory(BuiltInCategory.OST_MechanicalEquipment).WhereElementIsNotElementType().ToElements();\n    var flowValues = terminals.Select(x => x.LookupParameter("Flow")?.AsDouble()).ToList();\n}'},
    'Create an MEP Coordination 3D View': {'discipline': 'MEP', 'prompt': 'Create a 3D view named MEP Coordination for multidisciplinary coordination.', 'code': 'using System;\nusing Autodesk.Revit.DB;\nusing System.Linq;\n\n// AuroraRevit v1.8.3 educational template: Create an MEP Coordination 3D View\n// This template is intentionally read-only or creates a single named view/schedule.\npublic static void Run(Document doc)\n{\n    var category = new ElementId(BuiltInCategory.OST_DuctCurves);\n    var viewType = new FilteredElementCollector(doc).OfClass(typeof(ViewFamilyType)).Cast<ViewFamilyType>().FirstOrDefault(x => x.ViewFamily == ViewFamily.ThreeDimensional);\n    if (viewType == null) return;\n    var view = View3D.CreateIsometric(doc, viewType.Id);\n    view.Name = "MEP Coordination";\n}'},
    'Create a Pipe Schedule': {'discipline': 'MEP', 'prompt': 'Create a schedule named P-PIPE for all pipe segments.', 'code': 'using System;\nusing Autodesk.Revit.DB;\nusing System.Linq;\n\n// AuroraRevit v1.8.3 educational template: Create a Pipe Schedule\n// This template is intentionally read-only or creates a single named view/schedule.\npublic static void Run(Document doc)\n{\n    var category = new ElementId(BuiltInCategory.OST_PipeCurves);\n    var schedule = ViewSchedule.CreateSchedule(doc, category);\n    schedule.Name = "P-PIPE";\n}'},
    'Create a Concrete Column Schedule': {'discipline': 'Structure', 'prompt': 'Create a schedule named S-COL for structural columns with type and base level.', 'code': 'using System;\nusing Autodesk.Revit.DB;\nusing System.Linq;\n\n// AuroraRevit v1.8.3 educational template: Create a Concrete Column Schedule\n// This template is intentionally read-only or creates a single named view/schedule.\npublic static void Run(Document doc)\n{\n    var category = new ElementId(BuiltInCategory.OST_StructuralColumns);\n    var schedule = ViewSchedule.CreateSchedule(doc, category);\n    schedule.Name = "S-COL";\n}'},
    'Create a Structural Beam Schedule': {'discipline': 'Structure', 'prompt': 'Create a schedule named S-BEAM for structural framing members.', 'code': 'using System;\nusing Autodesk.Revit.DB;\nusing System.Linq;\n\n// AuroraRevit v1.8.3 educational template: Create a Structural Beam Schedule\n// This template is intentionally read-only or creates a single named view/schedule.\npublic static void Run(Document doc)\n{\n    var category = new ElementId(BuiltInCategory.OST_StructuralFraming);\n    var schedule = ViewSchedule.CreateSchedule(doc, category);\n    schedule.Name = "S-BEAM";\n}'},
    'Find Beams with No Structural Usage': {'discipline': 'Structure', 'prompt': 'Find structural framing elements whose Structural Usage parameter is empty.', 'code': 'using System;\nusing Autodesk.Revit.DB;\nusing System.Linq;\n\n// AuroraRevit v1.8.3 educational template: Find Beams with No Structural Usage\n// This template is intentionally read-only or creates a single named view/schedule.\npublic static void Run(Document doc)\n{\n    var category = new ElementId(BuiltInCategory.OST_StructuralFraming);\n    var beams = new FilteredElementCollector(doc).OfCategory(BuiltInCategory.OST_StructuralFraming).WhereElementIsNotElementType().ToElements();\n    var missing = beams.Where(x => string.IsNullOrWhiteSpace(x.LookupParameter("Structural Usage")?.AsValueString())).ToList();\n}'},
    'Set Column Comments': {'discipline': 'Structure', 'prompt': 'Set the Comments parameter of selected structural columns to Foundation Review.', 'code': 'using System;\nusing Autodesk.Revit.DB;\nusing System.Linq;\n\n// AuroraRevit v1.8.3 educational template: Set Column Comments\n// This template is intentionally read-only or creates a single named view/schedule.\npublic static void Run(Document doc)\n{\n    var category = new ElementId(BuiltInCategory.OST_StructuralColumns);\n    var columns = new FilteredElementCollector(doc).OfCategory(BuiltInCategory.OST_StructuralColumns).WhereElementIsNotElementType().ToElements();\n    var editable = columns.Where(x => x.LookupParameter("Comments") != null).ToList();\n}'},
    'Count Structural Framing by Type': {'discipline': 'Structure', 'prompt': 'Count structural framing elements grouped by type.', 'code': 'using System;\nusing Autodesk.Revit.DB;\nusing System.Linq;\n\n// AuroraRevit v1.8.3 educational template: Count Structural Framing by Type\n// This template is intentionally read-only or creates a single named view/schedule.\npublic static void Run(Document doc)\n{\n    var category = new ElementId(BuiltInCategory.OST_StructuralFraming);\n    var framing = new FilteredElementCollector(doc).OfCategory(BuiltInCategory.OST_StructuralFraming).WhereElementIsNotElementType().ToElements();\n    var counts = framing.GroupBy(x => x.GetTypeId()).Select(g => new { TypeId = g.Key, Count = g.Count() }).ToList();\n}'},
    'Create a Structural Foundation Schedule': {'discipline': 'Structure', 'prompt': 'Create a schedule named S-FOUNDATION for structural foundations.', 'code': 'using System;\nusing Autodesk.Revit.DB;\nusing System.Linq;\n\n// AuroraRevit v1.8.3 educational template: Create a Structural Foundation Schedule\n// This template is intentionally read-only or creates a single named view/schedule.\npublic static void Run(Document doc)\n{\n    var category = new ElementId(BuiltInCategory.OST_StructuralFoundation);\n    var schedule = ViewSchedule.CreateSchedule(doc, category);\n    schedule.Name = "S-FOUNDATION";\n}'},
    'Find Beams Above Level 2': {'discipline': 'Structure', 'prompt': 'Find structural beams whose elevation is above Level 2.', 'code': 'using System;\nusing Autodesk.Revit.DB;\nusing System.Linq;\n\n// AuroraRevit v1.8.3 educational template: Find Beams Above Level 2\n// This template is intentionally read-only or creates a single named view/schedule.\npublic static void Run(Document doc)\n{\n    var category = new ElementId(BuiltInCategory.OST_StructuralFraming);\n    var level = new FilteredElementCollector(doc).OfClass(typeof(Level)).Cast<Level>().FirstOrDefault(x => x.Name == "Level 2");\n    if (level == null) return;\n    var beams = new FilteredElementCollector(doc).OfCategory(BuiltInCategory.OST_StructuralFraming).WhereElementIsNotElementType().Where(x => (x.Location as LocationCurve)?.Curve?.GetEndPoint(0).Z > level.Elevation).ToList();\n}'},
    'Create a Structural 3D Coordination View': {'discipline': 'Structure', 'prompt': 'Create a dedicated 3D view named S-Coordination.', 'code': 'using System;\nusing Autodesk.Revit.DB;\nusing System.Linq;\n\n// AuroraRevit v1.8.3 educational template: Create a Structural 3D Coordination View\n// This template is intentionally read-only or creates a single named view/schedule.\npublic static void Run(Document doc)\n{\n    var category = new ElementId(BuiltInCategory.OST_StructuralFraming);\n    var viewType = new FilteredElementCollector(doc).OfClass(typeof(ViewFamilyType)).Cast<ViewFamilyType>().FirstOrDefault(x => x.ViewFamily == ViewFamily.ThreeDimensional);\n    if (viewType == null) return;\n    var view = View3D.CreateIsometric(doc, viewType.Id);\n    view.Name = "S-Coordination";\n}'},
    'Schedule Rebar by Bar Diameter': {'discipline': 'Structure', 'prompt': 'Create a schedule named S-REBAR grouped by rebar bar diameter.', 'code': 'using System;\nusing Autodesk.Revit.DB;\nusing System.Linq;\n\n// AuroraRevit v1.8.3 educational template: Schedule Rebar by Bar Diameter\n// This template is intentionally read-only or creates a single named view/schedule.\npublic static void Run(Document doc)\n{\n    var category = new ElementId(BuiltInCategory.OST_Rebar);\n    var schedule = ViewSchedule.CreateSchedule(doc, category);\n    schedule.Name = "S-REBAR";\n}'},
    'Select Structural Columns at Level 1': {'discipline': 'Structure', 'prompt': 'Select all structural columns associated with Level 1.', 'code': 'using System;\nusing Autodesk.Revit.DB;\nusing System.Linq;\n\n// AuroraRevit v1.8.3 educational template: Select Structural Columns at Level 1\n// This template is intentionally read-only or creates a single named view/schedule.\npublic static void Run(Document doc)\n{\n    var category = new ElementId(BuiltInCategory.OST_StructuralColumns);\n    var columns = new FilteredElementCollector(doc).OfCategory(BuiltInCategory.OST_StructuralColumns).WhereElementIsNotElementType().ToElements();\n    var levelOne = columns.Where(x => x.LevelId != ElementId.InvalidElementId).ToList();\n}'},
}

__all__ = ["ACTION_TEMPLATES"]
